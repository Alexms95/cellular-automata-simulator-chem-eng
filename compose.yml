services:
  frontend:
    build: .
    container_name: frontend
    ports:
      - "80:80"
    depends_on:
      - backend
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: backend-container
    container_name: backend
    ports:
      - "8000:8000"