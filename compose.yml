services:
  backend:
    image: alexms/api-simulador-automatos-celulares:1.0.0
    container_name: fastapi-backend
    ports:
      - "8000:8000"
    environment:
      - ENV=development
  nginx:
    image: alexms/front-simulador-automatos-celulares:1.0.0
    container_name: nginx
    ports:
      - "9000:80"
    depends_on:
      - backend
